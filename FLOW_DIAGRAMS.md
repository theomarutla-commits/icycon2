# Button & User Data Flow - Visual Guide

## Login Button Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ User Clicks "Login" on Landing Page                             │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ Header.tsx onClick handler calls onLogin()                      │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ App.tsx route calls navigate('/login')                          │
│ ✅ Uses React Router for smooth navigation                      │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ React Router changes URL to /login                              │
│ ✅ No full page reload                                          │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ AuthPage renders SignupPage with isLogin=true                   │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ AuthForm renders Login form                                     │
│ - Email input                                                   │
│ - Password input                                                │
│ - Submit button                                                 │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ User fills form and clicks submit                               │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ AuthForm.handleSubmit() fires                                   │
│ - Validates inputs                                              │
│ - Calls onSubmit(email, password)                               │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ SignupPage.handleAuth() executes                                │
│ - Sets loading = true                                           │
│ - Calls api.login(email, password)                              │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ api.login() in auth.ts:                                         │
│ - Sends POST /users/api/auth/login                              │
│ - Django authenticates user                                     │
│ - Returns { token: "abc123...", user: {...} }                   │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ SignupPage receives response                                    │
│ ✅ Calls login(response.user, response.token)                   │
│    NOT: login(user) ❌ (THIS WAS THE BUG!)                       │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ AuthContext.login() executes:                                   │
│ - setUser(user)                                                 │
│ - setToken(token)                                               │
│ - localStorage.setItem('authToken', token)                      │
│ - localStorage.setItem('user', JSON.stringify(user))            │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ AuthContext state updates:                                      │
│ - user = { email: "user@example.com", username: "john" }        │
│ - token = "abc123def456..."                                     │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ useAuth() in App.tsx detects user !== null                      │
│ - AuthPage no longer needed                                     │
│ - Redirect to protected route /app                              │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────────────────────────┐
│ App.tsx renders DashboardPage                                   │
│ - AppNavbar shows user email                                    │
│ - DashboardSidebar shows navigation                             │
│ - Main content area ready                                       │
└────────────┬────────────────────────────────────────────────────┘
             │
             ↓
        ✅ SUCCESS!
   User is logged in and on dashboard
```

---

## User Data Display Flow

```
┌──────────────────────────────────────────────────┐
│ After Login, User Data Available                 │
└────────────┬─────────────────────────────────────┘
             │
             ↓
┌──────────────────────────────────────────────────┐
│ localStorage now contains:                       │
│ - authToken: "abc123def456..."                   │
│ - user: '{"email":"user@test.com","username"...'│
└────────────┬─────────────────────────────────────┘
             │
             ↓
┌──────────────────────────────────────────────────┐
│ Any Component can call:                          │
│                                                  │
│ const { user } = useAuth();                      │
│                                                  │
│ Now user contains:                               │
│ - user.email ✅                                  │
│ - user.username ✅                               │
└────────────┬─────────────────────────────────────┘
             │
             ↓
┌──────────────────────────────────────────────────┐
│ Examples of accessing user data:                 │
│                                                  │
│ Header.tsx:                                      │
│   <span>{user?.email}</span>                     │
│   ✅ Displays: user@test.com                     │
│                                                  │
│ ProfilePage.tsx:                                 │
│   <p>Name: {user?.username}</p>                  │
│   ✅ Displays: john                              │
│                                                  │
│ AnyPage.tsx:                                     │
│   const email = user?.email;                     │
│   ✅ Can use email for personalization           │
└────────────┬─────────────────────────────────────┘
             │
             ↓
        ✅ User data available everywhere!
```

---

## Session Persistence Flow

```
┌──────────────────────────────────────────────────┐
│ User Logs In Successfully                        │
│ ✅ localStorage populated with token and user    │
└────────────┬─────────────────────────────────────┘
             │
             ↓
┌──────────────────────────────────────────────────┐
│ User Refreshes Page (F5)                         │
└────────────┬─────────────────────────────────────┘
             │
             ↓
┌──────────────────────────────────────────────────┐
│ App mounts, AuthContext initializes              │
│ - AuthProvider renders                           │
│ - useEffect hook runs                            │
└────────────┬─────────────────────────────────────┘
             │
             ↓
┌──────────────────────────────────────────────────┐
│ AuthContext useEffect:                           │
│                                                  │
│ const storedToken =                              │
│   localStorage.getItem('authToken')              │
│ // Returns: "abc123def456..."                    │
│                                                  │
│ const storedUser =                               │
│   localStorage.getItem('user')                   │
│ // Returns: '{"email":"...","username":"..."}'  │
└────────────┬─────────────────────────────────────┘
             │
             ↓
┌──────────────────────────────────────────────────┐
│ AuthContext updates state:                       │
│ - setToken(storedToken)                          │
│ - setUser(JSON.parse(storedUser))                │
│ - setIsLoading(false)                            │
└────────────┬─────────────────────────────────────┘
             │
             ↓
┌──────────────────────────────────────────────────┐
│ App.tsx checks isLoading:                        │
│ - isLoading = false                              │
│ - Renders routes normally                        │
└────────────┬─────────────────────────────────────┘
             │
             ↓
┌──────────────────────────────────────────────────┐
│ useAuth() in App.tsx:                            │
│ - Returns user !== null                          │
│ - Allows access to /app routes                   │
└────────────┬─────────────────────────────────────┘
             │
             ↓
┌──────────────────────────────────────────────────┐
│ DashboardPage renders:                           │
│ - user data available                            │
│ - navbar shows email                             │
│ - all pages accessible                           │
└────────────┬─────────────────────────────────────┘
             │
             ↓
        ✅ User still logged in after refresh!
```

---

## Navigation Flow (React Router)

```
┌──────────────────────────────┐
│ User on Landing Page         │
└────────┬─────────────────────┘
         │
         ├─ Clicks "Login"
         │
         ↓
┌──────────────────────────────┐
│ onLogin() callback fires     │
│ ✅ Uses React Router navigate│
│ navigate('/login')           │
└────────┬─────────────────────┘
         │
         ├─ URL changes to /login
         │
         ├─ No page reload
         │
         ├─ React state preserved
         │
         ↓
┌──────────────────────────────┐
│ AuthPage renders             │
│ with isLogin = true          │
└────────┬─────────────────────┘
         │
         ├─ User fills login form
         │
         ├─ Submits credentials
         │
         ↓
┌──────────────────────────────┐
│ Successful login             │
│ navigate('/app') called      │
└────────┬─────────────────────┘
         │
         ├─ URL changes to /app
         │
         ├─ No page reload
         │
         ├─ React state preserved
         │
         ↓
┌──────────────────────────────┐
│ DashboardPage renders        │
│ Smooth transition!           │
└──────────────────────────────┘
```

---

## Before vs After Comparison

### BEFORE (❌ Broken)
```
Login Button
    ↓
window.location.href = '/login'
    ↓
Full page reload ❌
    ↓
React state lost ❌
    ↓
SlowUser Experience ❌
    ↓
AuthContext login() only gets user ❌
    ↓
Token not passed ❌
    ↓
User data not available ❌
```

### AFTER (✅ Fixed)
```
Login Button
    ↓
navigate('/login') ✅
    ↓
URL changes smoothly ✅
    ↓
React state preserved ✅
    ↓
Fast User Experience ✅
    ↓
AuthContext login(user, token) ✅
    ↓
Token properly passed ✅
    ↓
User data available everywhere ✅
```

---

## Key Improvements

| Aspect | Before | After |
|--------|--------|-------|
| **Navigation** | Full page reload | React Router smooth |
| **Performance** | Slow | Fast |
| **UX** | Jarring | Smooth |
| **State** | Lost | Preserved |
| **User Data** | Not available | Available |
| **Token** | Not passed | Properly passed |
| **Email Display** | Not showing | Shows in navbar |

---

## Testing the Flows

### Test Login Flow
1. ✅ Click Login button
2. ✅ Navigate to /login (smooth, no reload)
3. ✅ Submit form
4. ✅ Navigate to /app (smooth)
5. ✅ See dashboard with email

### Test Data Display
1. ✅ Login successfully
2. ✅ Check navbar
3. ✅ See email displayed
4. ✅ Open DevTools → Application
5. ✅ See user data in localStorage

### Test Session Persistence
1. ✅ Login successfully
2. ✅ Press F5 refresh
3. ✅ Still on /app (no redirect)
4. ✅ Email still shows
5. ✅ User data still available

---

## Architecture Summary

```
Frontend (React)
├── Landing Page
│   └── Navigation Buttons (use React Router) ✅
├── Login/Signup Pages
│   └── Auth Forms (call api.login/signup) ✅
├── Dashboard Pages
│   └── Display user email (from useAuth) ✅
└── Context
    └── AuthContext (manages token + user) ✅

↓ (Smooth Navigation - no reloads)

Backend (Django)
├── /users/api/auth/login
│   └── Returns { token, user } ✅
├── /users/api/auth/signup
│   └── Returns { token, user } ✅
└── /api/...
    └── Uses Authorization header ✅
```

All flows working correctly! 🎉
