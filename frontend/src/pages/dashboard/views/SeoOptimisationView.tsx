import React from 'react';
import PlaceholderView from './PlaceholderView';

const SeoOptimisationView = () => (
    <PlaceholderView title="SEO Tools" subtitle="Manage sites, keywords, and content." />
);

export default SeoOptimisationView;